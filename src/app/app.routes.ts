import { Routes } from '@angular/router';
import { CarListingsComponent } from './car-listings/car-listings.component';

export const routes: Routes = [
  { path: '', component: CarListingsComponent }
];
